// utils/helpers.js

/// @notice Maneja errores de manera centralizada.
/// @dev Imprime en consola el mensaje de error recibido.
/// @param err Objeto de error capturado (debe contener la propiedad `message`).
/// @return No retorna valores; únicamente muestra el error en consola.
export function handleError(err) {
  console.error("❌ Error:", err.message);
}
// utils/helpers.js

/// @notice Maneja errores de manera centralizada.
/// @dev Imprime en consola el mensaje de error recibido.
/// @param err Objeto de error capturado (debe contener la propiedad `message`).
/// @return No retorna valores; únicamente muestra el error en consola.
export function handleError(err) {
  console.error("❌ Error:", err.message);
}
